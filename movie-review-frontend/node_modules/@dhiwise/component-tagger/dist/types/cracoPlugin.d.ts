import { ComponentTagOptions } from './types';
export declare function createCracoPlugin(options?: ComponentTagOptions): {
    plugin: {
        overrideWebpackConfig: ({ webpackConfig }: {
            webpackConfig: any;
        }) => any;
    };
};
